# CONFIG365 Preview

This is the **preview** channel for CONFIG365 — bleeding-edge builds before GA promotion.

**Deploy:** see [docs/DEPLOY.md](docs/DEPLOY.md). Local run: `docker compose up -d --build` only (port **8080** by default).

**Stable releases:** [potsolutions/config365](https://github.com/potsolutions/config365).
