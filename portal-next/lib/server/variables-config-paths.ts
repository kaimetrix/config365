/** Canonical path in the MSP baseline Gitea repo (repo root, not under baseline/). */
export const VARIABLES_CONFIG_PATH = 'variables.json';

/** Per-tenant override file in each tenant-{slug} repo. */
export const TENANT_VARIABLES_PATH = 'config/variables.json';
