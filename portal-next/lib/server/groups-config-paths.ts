/** Canonical path in the MSP baseline Gitea repo (repo root, not under baseline/). */
export const GROUPS_CONFIG_PATH = 'groups-config.json';

/** Import used to write here before repo-root hoisting — still read for migration. */
export const GROUPS_CONFIG_LEGACY_PATH = 'baseline/groups-config.json';
